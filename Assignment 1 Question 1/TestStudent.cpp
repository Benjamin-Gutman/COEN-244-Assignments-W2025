//Benjamin Gutman 40315265
//Jiyong Jeon 40314593

#include <iostream>
#include <string>
#include "Student.h"

using namespace std;

/*int main() {
    string studentId = "40314593";
    string firstName = "Jiyong";
    string lastName = "Jeon";
    string address = "69 rue saint cathrine, montreal, QC, Canada, H2E 0L3";
    string email = "jiyong@gmail.com";

    // Creating an empty student object and a parameterized one
    Student student1;
    Student student2(studentId, firstName, lastName, address, email);

    // Testing setters and getters for student1
    student1.set_StudentID(studentId);
    if (studentId == student1.getStudentID()) {
        cout << "Student ID test passed" << endl;
    }

    student1.set_FirstName(firstName);
    if (firstName == student1.getFirstName()) {
        cout << "First Name test passed" << endl;
    }

    student1.set_LastName(lastName);
    if (lastName == student1.getLastName()) {
        cout << "Last Name test passed" << endl;
    }

    student1.set_Address(address);
    if (address == student1.getAddress()) {
        cout << "Address test passed" << endl;
    }

    student1.set_Email(email);
    if (email == student1.getEmail()) {
        cout << "Email test passed" << endl;
    }

    // Testing parameterized constructor for student2
    if (studentId == student2.getStudentID()) {
        cout << "Student ID (constructor) test passed" << endl;
    }

    if (firstName == student2.getFirstName()) {
        cout << "First Name (constructor) test passed" << endl;
    }

    if (lastName == student2.getLastName()) {
        cout << "Last Name (constructor) test passed" << endl;
    }

    if (address == student2.getAddress()) {
        cout << "Address (constructor) test passed" << endl;
    }

    if (email == student2.getEmail()) {
        cout << "Email (constructor) test passed" << endl;
    }

    // Printing student information
    cout << "\nPrinting Student1 Information:\n";
    student1.printStudentInfo();

    cout << "\nPrinting Student2 Information:\n";
    student2.printStudentInfo();

    return 0;
}*/
